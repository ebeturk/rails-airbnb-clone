<h1>My Beasts 🐲</h1>

<ul>  <% @dragons.each  do |dragon| %>
    <li>
      <%= link_to dragon.name, dragon_path(dragon) %>
      <br>
      <%= link_to "Edit the Dragon", edit_dragon_path(dragon), class: 'btn btn-light btn-sm border' %>
    </li>
  <% end %>
</ul>
