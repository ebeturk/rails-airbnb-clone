<%= simple_form_for [dragon, booking] do |f| %>
  <%= f.input :start_date, as: :date, html5: true, order: [:day, :month, :year] %>
  <%= f.input :end_date, as: :date, html5: true, order: [:day, :month, :year] %>
  <%= f.button :submit %>
<% end %>
